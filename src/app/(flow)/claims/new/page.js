import ClaimsIntakeFlow from '@/components/claims/ClaimsIntakeFlow';

export const metadata = { title: 'Start a Claim — ClaimPoint Solutions' };

export default function NewClaimPage() {
  return <ClaimsIntakeFlow />;
}